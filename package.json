{
  "name": "transaku",
  "version": "0.1.0",
  "private": true,
  "description": "Transaku — pencatatan transaksi lokal.",
  "scripts": {
    "start": "npx serve www -l 5173",
    "cap:sync": "npx cap sync android",
    "cap:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor-community/sqlite": "^8.1.1",
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "html2canvas": "^1.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "serve": "^14.2.4"
  }
}
